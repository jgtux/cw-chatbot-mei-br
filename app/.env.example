# Copy to .env and fill in real values. SECRET_KEY and JWT_SECRET_KEY are
# required -- the app will refuse to start without them (see config.py).
SECRET_KEY=
JWT_SECRET_KEY=
FLASK_ENV=development
MONGO_URL=mongodb://localhost:27017/
MONGO_DB_NAME=db
GROQ_API_KEY=
